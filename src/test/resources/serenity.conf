webdriver {
  driver = chrome
  chrome.driver = provided
      capabilities {
          "goog:chromeOptions": {
              args: ["--start-maximized", "--disable-extensions"]
          }
      }
}
serenity {
    timeout = 30,
    serenity.keep.driver.open=true
}

